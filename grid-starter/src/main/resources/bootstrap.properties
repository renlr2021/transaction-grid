server.port = ${APP_PORT:15089}
spring.application.name = ${APP_NAME:transaction-grid}


env=${ENV:DEV}
app.id=${APP_ID:111000}
apollo.meta=${APOLLO_URL:http://192.168.50.242:18628}
apollo.bootstrap.enabled = ${APOLLO_ENABLE:true}
apollo.bootstrap.namespaces = ${NAMESPACE:application}


eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=${IP:127.0.0.1}
eureka.instance.instance-id=${eureka.instance.ip-address}:${server.port}
eureka.client.service-url.defaultZone=http://192.168.50.242:8520/eureka/


feign.cat.client.enabled=true
spring.profiles.active=swagger,task